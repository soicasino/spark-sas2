# Core packages for SAS protocol and serial communication
pyserial>=3.5
crccheck>=1.0

# System utilities
psutil>=5.8
distro>=1.6  # Linux only

# Database connectivity
pymssql>=2.2
psycopg2-binary>=2.9

# GUI frameworks
PyQt5>=5.15
wxpython>=4.1  # For HTML GUI support (works on all platforms)
cefpython3>=66.0  # For CEF browser support (x86_64 only, not available on ARM/Raspberry Pi)
pywebview>=3.6

# Web framework
Flask>=2.0
flask-restful>=0.3

# Optional: Development and debugging tools
# requests>=2.28  # For HTTP requests if needed
# python-dateutil>=2.8  # For date handling 