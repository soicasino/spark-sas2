# Safer requirements.txt for Jetson Nano
# Prioritizes packages that compile cleanly on ARM64

# Core packages for SAS protocol and serial communication
pyserial==3.5
crccheck==1.3.0
netifaces==0.11.0

# System utilities
psutil>=5.8.0
distro>=1.6.0

# FastAPI and web server (minimal dependencies)
fastapi==0.104.1
uvicorn==0.24.0  # Without [standard] to avoid compilation issues
pydantic==2.5.0
websockets==12.0

# Basic HTTP client
httpx==0.25.2

# Configuration and environment
python-dotenv==1.0.0

# Logging and monitoring
structlog==23.2.0

# Multipart form handling (for FastAPI)
python-multipart==0.0.6

# Development and testing
pytest==7.4.3
pytest-asyncio==0.21.1

# Authentication - Simpler alternatives
# Use these instead of python-jose and bcrypt if compilation fails:
pyjwt>=2.8.0  # Pure Python JWT library
argon2-cffi>=23.0.0  # Password hashing (easier to compile than bcrypt)

# If you need the original auth packages, try these after system deps are installed:
# python-jose[cryptography]==3.3.0
# passlib[bcrypt]==1.7.4